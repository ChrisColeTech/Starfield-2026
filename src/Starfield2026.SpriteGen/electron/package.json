{
    "name": "spritegen-electron",
    "private": true,
    "version": "0.0.0",
    "main": "main.js",
    "scripts": {
        "dev": "wait-on http://localhost:5173 && electron ."
    },
    "devDependencies": {
        "electron": "^35.1.2",
        "wait-on": "^9.0.4"
    },
    "dependencies": {
        "electron-store": "^8.2.0"
    }
}